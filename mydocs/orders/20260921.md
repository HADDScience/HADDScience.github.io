---
kind: snapshot
status: active
canonical: AGENTS.md
last_verified: 2026-09-21
---

# 2026-09-21

| 상태 | 일 |
|---|---|
| 완료 | 관리 화면 목록이 오른쪽으로 삐져나가던 것을 고침 — 목록 `li` 와 편집기 상단 바에 `min-w-0` (PR #4) |
| 완료 | 관리 화면에 콘텐츠 현황 패널 — 타일 넷 · "손볼 것" 필터 칩 · 월별 발행 막대 |

## 목록이 화면 밖으로 나간 이유

`ul.grid` 안의 `li` 는 그리드 항목이라 최소 너비가 `auto` 다. `li` 의 최소 너비는 그 안의
flex 줄의 min-content 인데, 제목 `<p>` 가 `truncate`(=`white-space: nowrap`)라 **제목 한 줄
전체 너비**가 그대로 최소 너비가 된다. 제목 안쪽 `<button>` 의 `min-w-0` 은 flex 레이아웃에서
줄여 주기는 해도 min-content 계산에는 쓰이지 않는다. 그래서 긴 제목 한 건이 목록 전체를
칸 밖으로 밀어낸다 — 한 건만 길어도 모든 줄이 같이 넓어진다(그리드 칸을 공유한다).

`li` 에 `min-w-0` 을 주면 칸 너비까지 줄어들고, 그 안에서 제목이 원래 의도대로 말줄임된다.
같은 구조가 편집기 상단 바(`post-editor` · `deck-editor`)에도 있어 함께 고쳤다 — 제목이 길면
sticky 바가 화면 밖으로 나간다.

실측(뷰포트 1700px · 컨테이너 1104px, Omnis API 를 목킹한 Playwright):

```
고치기 전:  ul 1104  li 1341  → 237px 초과
고친 뒤:    ul 1104  li 1104  → 초과 없음, 긴 제목만 말줄임
```

품질 게이트 `pnpm typecheck && pnpm lint && pnpm build` 통과(exit 0).

## 콘텐츠 현황 패널

목록 위에 요약을 붙였다. 숫자는 이미 받아 둔 목록에서 센다 — 집계 API 를 따로 두면
화면의 목록과 다른 수가 나올 수 있고, 154건은 브라우저에서 세도 티가 나지 않는다.

"손볼 것" 칩(번역 없음 · 본문 없음 · 썸네일 없음 · 요약 없음)은 누르면 목록이 그 글만
남는다. 결함 정의는 `matchesIssue()` 한 곳에 있고 패널의 숫자와 목록 필터가 같은 함수를
쓴다. 본문이 없는 글은 번역·요약에서 빼, 한 글이 칩 셋에 동시에 잡히지 않게 했다.

월별 막대는 한 갈래·한 색(브랜드 블루)이고 범례가 없다. 값은 hover 로 읽는다.

목킹 데이터(라이브러리 21 · 뉴스 2)로 확인한 것: 타일 수치(카드뉴스 4건 12장 ·
번역 19/21 90% · 최근 12개월 20건 · 마지막 글 2026.09.02), 칩 필터(번역 없음 2 →
목록 2건), 0건 칩 비활성, 탭 전환 시 필터 해제, 막대 hover 툴팁(`2026-06 · 2건`),
390px 폭에서 가로 넘침 없음.
