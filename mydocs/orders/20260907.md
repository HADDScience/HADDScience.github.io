---
kind: snapshot
status: active
canonical: AGENTS.md
last_verified: 2026-09-07
---

# 2026-09-07

| 상태 | 일 |
|---|---|
| 완료 | 카드뉴스 편집기 — `/admin` 에서 9종 레이아웃을 폼으로 채워 1080×1080 으로 굽고, 기사 게시 + PNG zip (`1e0c5be`) |
| 완료 | `/admin` 로그인을 GitHub PAT 에서 Omnis SSO 로 (`917a99e` · `08a596d`) |
| 완료 | 기사·사진 저장소를 git 에서 Omnis(Neon · NAS)로. 정적 export 해제, 서버 렌더 + ISR (`9a40874`) |
| 완료 | Vercel 배포 정상화 — framework 명시 · 허브 빌드 격리 · rewrite 를 next.config 로 (`73604f0`~`cb70771`) |
| 완료 | `mydocs/` 체계 도입 (이 문서) |
| 완료 | 카드뉴스 편집기 — 사진이 없는 자리를 미리보기에서 점선 상자로 (결과물에는 안 찍힘) |
| 확인 대기 | 실제 계정으로 `haddscience.vercel.app/admin` 로그인 → 저장 → 사이트 반영. AI 는 계정이 없어 못 했다 |
| 미착수 | 옛 `content/data/news/` · `public/news/` 삭제 — 위 확인 뒤 승인받고 |
| 미착수 | `haddscience.com` DNS 를 Vercel 로 · `DEPLOY_TARGET=pages` 제거(색인 허용) |
| 미착수 | 카드뉴스 영문 자동 생성 (굽는 단계가 브라우저에만 있다) |
