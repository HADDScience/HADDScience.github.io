---
kind: snapshot
status: active
canonical: AGENTS.md
last_verified: 2026-09-23
---

# 2026-09-23

| 상태 | 일 |
|---|---|
| 완료 | Omnis 가 답하지 않을 때 빌드와 문의 폼이 매달리지 않게 한다 (PR #13 · 머지 `1982762` · 배포 확인) |
| 완료 | 방침이 약속한 보존기간을 실제로 지우는 것 — Omnis PR #66 머지·배포 (`omnis-local-50`) |

## 타임아웃 — 왜 필요했나

Node 의 `fetch` 에는 기본 타임아웃이 없다. 소켓이 끊기지 않으면 요청이 영원히 매달린다.

기사 목록(`content/server.ts` → `GET {Omnis}/posts`)은 빌드가 361쪽을 만드는 재료다.
그 한 건이 매달리면 빌드 전체가 Vercel 의 45분 한도까지 간다. 이 계정은 **동시 빌드가 1개**라
그동안 회사의 다른 배포도 함께 막힌다 — 2026-09-22 에 다른 브랜치의 빌드 하나가 35분 물려
세 프로젝트(haddscience · omnis-hadd · omnis-demo)의 프리뷰가 전부 섰다.

같은 날 Omnis DB 가 Neon 무료 한도 초과로 거절을 내고 있었다([그날 기록](../working/2026-09-22-seo-and-hero-flip.md)).
이 경로가 느려지는 일은 가정이 아니라 이미 한 번 일어난 일이다.

문의 라우트도 같은 모양이었다. Omnis 가 답하지 않으면 방문자의 폼이 끝없이 도는 상태로 남아
**메일 되돌림조차 보지 못한다.** 그 되돌림을 만든 이유가 바로 이 경우인데 거기까지 가지 못했다.

## 고친 방법

`lib/omnis-fetch.ts` 하나를 두고 `content/server.ts` 와 `app/api/contact` 가 쓴다.

**타임아웃 뒤 한 번 더 시도한다.** 타임아웃만 두면 잠깐의 느려짐이 곧바로 실패가 된다 —
기사 없는 사이트, 문의 접수 거부. POST 재시도가 문의를 둘로 쌓지 않는 근거는 Omnis 의
멱등성(5분 안의 같은 이메일·같은 본문 → `200 duplicate`)이다. 그 장치가 여기서 값을 했다.

**기다리는 쪽에 따라 한도를 다르게 뒀다.**

| 부르는 곳 | 한 번 | 최악 | 왜 |
|---|---|---|---|
| 기사 목록 (빌드·ISR) | 10초 | 20초 | 기다리는 것이 빌드다 |
| 문의 접수 | 5초 | 10초 | 기다리는 것이 사람이다. 운영 왕복은 1초대였다 |

처음에 문의 쪽도 10초로 뒀다가 실측에서 방문자가 21초를 기다리는 것을 보고 5초로 줄였다.

## 실측

연결은 받고 영원히 응답하지 않는 서버를 3102 에 세워 재봤다.

```
맨 fetch            20초가 지나도 매달려 있음 (빌드라면 45분 한도까지)
omnisFetch          20.0s 만에 TimeoutError
                    blackhole 로그에 요청 두 번 — 재시도가 실제로 나갔다

빌드 (응답 없음)     종료코드 0 · 146초 · 기사 없는 사이트로 완주
빌드 (정상 운영)     종료코드 0 · 13초 · 실패 로그 0건       ← 느려지지 않았다
문의 (응답 없음)     503 {"error":"unavailable"} · 10초      ← 방문자가 메일 되돌림을 본다
```

빌드가 146초인 것은 Next 가 실패 응답을 캐시하지 않아 몇 갈래에서 다시 부르기 때문이다
(blackhole 이 39건 받음, 워커들이 병렬로 돈다). 무한히 매달리는 것과 비교할 값은 아니다.

배포 뒤 운영:

```
/ko/ /ko/news/ /ko/contact/ /ko/privacy/ /en/privacy/ /ko/terms/   전부 200
/ko/news/ 기사 상세 링크 13건 · HADD PICK 배지 3건   ← 목록이 비지 않았다
POST /api/contact  형식 오류 → 400 fields{…}
                   체류시간 100ms → 201 (봇 갈래, Omnis 로 가지 않음)
```

## 손대지 않은 것

Omnis 로 나가는 fetch 는 다섯이다. 둘만 고쳤다.

- `app/api/hit/route.ts`(방문 비콘) — **같은 문제가 있다.** 다른 작업의 파일이라 건드리지 않았다.
  빌드는 막지 않지만 함수가 응답까지 붙들린다.
- `lib/admin-api.ts` · `lib/omnis-auth.ts` — 브라우저에서 돌아 빌드를 막지 않는다.
  다만 관리 화면이 영원히 도는 상태가 될 수는 있다.

## 보존기간 파기 — Omnis 쪽에서 진행 중

방침(2026-09-22 공개)이 약속한 것을 실제로 지우는 것이 없었다.

| 무엇 | 방침에 적은 기간 | 상태 |
|---|---|---|
| 문의 | 접수일로부터 3년 | `scripts/purge-website-inquiries.ts` 는 있으나 **실제 삭제를 아무도 못 봄** (3년 지난 데이터가 없어 미리보기 0건) |
| 방문 통계 | 방문일로부터 400일 | 스크립트가 없었다 → `purge-website-visits.ts` 작성 중 |

### 확인된 것 (Omnis 세션 실측, 심은 행으로. 운영 데이터 아님)

```
문의 3년 — 다섯 갈래
  NEW · REJECTED · SPAM (3년+1일)   → 행째 사라짐
  ACCEPTED (3년+1일)                → 행은 남고 개인정보만 빔
                                      status · reviewedAt · orgId · contactId · quoteId 그대로
                                      이어진 기관 27곳 · 견적 14건 그대로
  NEW (3년−1일, 경계)               → 건드리지 않음

방문 통계 400일
  500일 · 401일  → 삭제
  399일 · 1일    → 남음
```

경계 한 건을 넣자고 한 이유는 기준이 `>` 인지 `>=` 인지보다 **하루 차이로 안 지워야 할 것이
지워지지 않는지**가 보고 싶어서였다. 지워지지 않았다.

### 방침 문구는 고치지 않았다

두 가지를 대조했다.

**6절 「개인정보의 파기」** — "보유 기간이 지나거나 처리 목적이 이루어진 개인정보를 지체 없이
파기합니다. 전자적 파일 형태의 정보는 복구할 수 없는 방법으로 삭제합니다." 이 조항은
**개인정보를 파기한다**이지 **행을 삭제한다**가 아니다. ACCEPTED 의 개인정보 칸을 비우는 방식과
어긋나지 않는다.

**400일** — 이 저장소가 지어낸 값이 아니었다. Omnis `mydocs/plans/2026-09-22-website-visit-stats.md:75`
가 "작년 같은 달과 비교하려면 1년 + 여유가 필요하다"는 근거와 함께 정한 값이고, 계획서에
`DELETE … interval '400 days'` 까지 적혀 있었다. 자동이 아니었을 뿐이다. 방침이 시스템을 따라간 것이 맞다.

### Omnis 쪽이 설계를 하나 더했다 — `reviewNote` 도 비운다

처음 합의에 없던 것이다. 담당자가 자유롭게 쓰는 칸이라 「전화로 통화, 김OO 교수」처럼
**방문자 개인정보가 들어갈 수 있다.** 방침이 3년 뒤 파기를 약속하는데 그 칸만 남으면 약속이
절반만 지켜진다. 잃는 것은 없다 — 「왜 그렇게 처리했는가」는 이어진 견적·샘플요청의 `note` 에,
「사람이 언제 처리했는가」는 `status`·`reviewedAt` 이 지킨다.

### 빈 이메일 걱정은 성립하지 않았다

ACCEPTED 를 비울 때 `email` 이 `""` 가 되는데, 그것이 중복 판정이나 레이트리밋에서 서로
같은 것으로 묶이지 않는지 물었다. 세 겹으로 막혀 있다 — 중복 판정과 레이트리밋 둘 다
시간 창(5분·24시간)을 함께 보므로 3년 된 행은 후보가 아니고, `inquiryIntakeSchema` 가 빈
이메일을 400 으로 막아 `""` 인 행이 **접수로** 생길 길 자체가 없다. 파기가 만드는 것뿐이다.

## 그래서 아직 남은 구멍 — 돌리는 사람이 없다

두 스크립트 다 **손으로 돌려야 한다.** 크론이 없다. 운영에서는 아직 한 번도 돌리지 않았는데,
지울 것이 없기 때문이다. 첫 대상이 생기는 날:

| 무엇 | 집계 시작 | 기간 | 첫 파기 대상 발생 |
|---|---|---|---|
| 방문 통계 | 2026-09-22 | 400일 | **2027-10-27** (오늘로부터 399일) |
| 문의 | 2026-09-22 | 3년 | **2029-09-22** |

방침 6절은 「지체 없이 파기합니다」라고 적혀 있다. 그 약속을 지키는 것이 지금은 **1년 뒤에
누군가 기억해 내는 것**뿐이다. 스크립트가 있는 것과 돌아가는 것은 다르다.

Omnis 쪽은 크론을 그때 정해도 늦지 않다고 보고 미뤘다. 첫 대상이 2027-10 이니 판단으로는
맞지만, 미룬 결정은 미뤘다고 적어 두지 않으면 결정한 적 없는 것이 된다. 그래서 여기 남긴다.

## 이 건은 여기서 닫는다

```
사이트   PR #10(문의·방침·약관) · #13(타임아웃) 머지·배포
Omnis    PR #58(문의함·API·알림·검토 화면) · #66(파기) 머지·배포
운영     POST /api/website/inquiries (비밀 없이) → 401     ← 503 이 아니다 = 비밀이 들어가 있다
         사이트 여섯 경로 200 · 문의 왕복 1건 확인 후 삭제
```

### 그래도 열려 있는 것

| 무엇 | 상태 |
|---|---|
| **방침·약관 법무 확인** | 2026-09-22 부터 공개돼 있고 변호사가 본 적이 없다 |
| **파기 크론** | 없다. 첫 대상 2027-10-27(방문) · 2029-09-22(문의) |
| `app/api/hit` 타임아웃 | 빌드는 막지 않지만 Omnis 가 답하지 않으면 함수가 응답까지 붙들린다 |

파기가 「돈다」는 것은 오늘 심은 행으로 확인했고, 「제때 돈다」는 것은 아직 아무 장치가 없다.
둘은 다른 말이다.
